# Composant fetch PJ Gmail (Plan A) -- dependances isolees de l'ETL/API.
# Install (poste Marlene, demain) : pip install -r requirements-gmail.txt
google-api-python-client>=2.130.0
google-auth-oauthlib>=1.2.0
google-auth>=2.30.0

# Parseur BL/PDF (src/scripts/gmail/parse_bl.py)
pdfplumber>=0.11.0
# OCR fallback (PDF scannés : BL signés / forwarders). Nécessite AUSSI les binaires
# système : tesseract-ocr (+ langue fra) et poppler.
pytesseract>=0.3.10
pdf2image>=1.17.0
